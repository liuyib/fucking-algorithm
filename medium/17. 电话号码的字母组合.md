# 17. 电话号码的字母组合

**递归**：

```js
let letterCombinations = function (digits) {
  // 输入空数据 '' 或空数组 [] 时
  if (digits.length === 0) {
    return [];
  }

  let phones = ['', '', 'abc', 'def', 'ghi', 'jkl', 'mno', 'pqrs', 'tuv', 'wxyz'];
  // 存储最终结果
  let result = [];

  let combination = (digits, index, letter) => {
    if (index === digits.length) {
      result.push(letter)
      return;
    }

    let letters = phones[digits[index]];
    for (let i = 0; i < letters.length; i++) {
      combination(digits, index + 1, letter + letters[i]);
    }
  };
  combination(digits, 0, '');

  return result;
};
```

**回溯**：

```js
let letterCombinations = function (digits) {
  // 输入空数据 '' 或空数组 [] 时
  if (digits.length === 0) {
    return [];
  }

  let phones = ['', '', 'abc', 'def', 'ghi', 'jkl', 'mno', 'pqrs', 'tuv', 'wxyz'];
  // 存储最终结果
  let result = [];
  // 存储回溯过程中的临时结果
  let current = [];

  let combination = (digits, index) => {
    // 当临时数组存入 digits.length 个数据时，递归返回
    if (index === digits.length) {
      result.push(current.join(''));
      return;
    }

    let letters = phones[digits[index]];
    for (let i = 0; i < letters.length; i++) {
      current.push(letters[i]);
      combination(digits, index + 1);
      // 开始回溯
      current.pop();
    }
  };
  combination(digits, 0);

  return result;
};
```
